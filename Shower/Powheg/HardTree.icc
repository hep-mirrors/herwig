// -*- C++ -*-
//
// This is the implementation of the inlined member functions of
// the HardTree class.
//

namespace Herwig {

inline NasonBranching::NasonBranching(ShowerParticlePtr particle,
				      SudakovPtr sudakov,
				      tNasonBranchingPtr parent,bool incoming) 
  : _particle(particle), _incoming(incoming), _parent(parent),
    _sudakov(sudakov)
{}

inline void NasonBranching::addChild(NasonBranchingPtr child) {
  _children.push_back(child);
}

inline ShowerParticlePtr NasonBranching::branchingParticle() {
    return _particle;
}

inline map<ShowerParticlePtr,tNasonBranchingPtr> & HardTree::particles() { 
  return _particles;
}

inline set<NasonBranchingPtr> & HardTree::branchings() {
  return _branchings;
}
inline set<NasonBranchingPtr> & HardTree::incoming() {
  return _spacelike;
}

inline void HardTree::connect(ShowerParticlePtr particle,NasonBranchingPtr branching) {
  if(_branchings.find(branching)==_branchings.end()) return;
  _particles[particle]=branching;
}
}
