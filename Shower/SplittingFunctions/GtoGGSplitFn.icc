// -*- C++ -*-
//
// This is the implementation of the inlined member functions of
// the GtoGGSplitFn class.
//

namespace Herwig {

inline GtoGGSplitFn::GtoGGSplitFn() : SplittingFunction(ShowerIndex::QCD) {}

inline GtoGGSplitFn::GtoGGSplitFn(const GtoGGSplitFn & x)
  : SplittingFunction(x) {}

inline IBPtr GtoGGSplitFn::clone() const {
  return new_ptr(*this);
}

inline IBPtr GtoGGSplitFn::fullclone() const {
  return new_ptr(*this);
}

inline void GtoGGSplitFn::doupdate() throw(UpdateException) {
  SplittingFunction::doupdate();
  // First update base class.
  bool redo = touched();
  // redo if touched.
//  UpdateChecker::check(aDependentMember, redo);
  // Update referenced objects on which this depends redo is set to true
  // if the dependent object is touched.
//  for_each(ContainerOfDependencies, UpdateChecker(redo));
  // Update a container of references.
//  for_each(MapOfDependencies, UpdateMapChecker(redo));
  // Update a map of references.
  if ( !redo ) return;
  // return if nothing has been touched. Otherwise do the actual update.
//  touch()
  // Touch if anything has changed.
}

inline void GtoGGSplitFn::doinit() throw(InitException) {
  SplittingFunction::doinit();
}

inline void GtoGGSplitFn::dofinish() {
  SplittingFunction::dofinish();
}

inline void GtoGGSplitFn::doinitrun() {
  SplittingFunction::doinitrun();
}

inline void GtoGGSplitFn::rebind(const TranslationMap & trans)
  throw(RebindException) {
  // dummy = trans.translate(dummy);
  SplittingFunction::rebind(trans);
}

inline IVector GtoGGSplitFn::getReferences() {
  IVector ret = SplittingFunction::getReferences();
  // ret.push_back(dummy);
  return ret;
}

}
