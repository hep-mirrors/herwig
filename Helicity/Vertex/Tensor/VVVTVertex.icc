// -*- C++ -*-
//
// This is the implementation of the inlined member functions of
// the VVVTVertex class.
//
namespace Herwig {
namespace Helicity {
using namespace ThePEG;
inline VVVTVertex::VVVTVertex() {}

inline VVVTVertex::VVVTVertex(const VVVTVertex & x)
  : VertexBase(x) {}

inline IBPtr VVVTVertex::clone() const {
  return new_ptr(*this);
}

inline IBPtr VVVTVertex::fullclone() const {
  return new_ptr(*this);
}

inline void VVVTVertex::doupdate() throw(UpdateException) {
  VertexBase::doupdate();
  // First update base class.
  bool redo = touched();
  // redo if touched.
//  UpdateChecker::check(aDependentMember, redo);
  // Update referenced objects on which this depends redo is set to true
  // if the dependent object is touched.
//  for_each(ContainerOfDependencies, UpdateChecker(redo));
  // Update a container of references.
//  for_each(MapOfDependencies, UpdateMapChecker(redo));
  // Update a map of references.
  if ( !redo ) return;
  // return if nothing has been touched. Otherwise do the actual update.
//  touch()
  // Touch if anything has changed.
}

inline void VVVTVertex::doinit() throw(InitException) {
  VertexBase::doinit();
}

inline void VVVTVertex::dofinish() {
  VertexBase::dofinish();
}

inline void VVVTVertex::doinitrun() {
  VertexBase::doinitrun();
}

inline void VVVTVertex::rebind(const TranslationMap & trans)
  throw(RebindException) {
  // dummy = trans.translate(dummy);
  VertexBase::rebind(trans);
}

inline IVector VVVTVertex::getReferences() {
  IVector ret = VertexBase::getReferences();
  // ret.push_back(dummy);
  return ret;
}

}
}
